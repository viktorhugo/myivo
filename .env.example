# docker-compose.yml — servicio postgres (debe coincidir con DATABASE_URL en apps/api/.env)
POSTGRES_USER=myivo
POSTGRES_PASSWORD=changeme
POSTGRES_DB=myivo

# Caddyfile — dominio real y correo para el certificado TLS al desplegar
APP_DOMAIN=localhost
CADDY_ACME_EMAIL=tu-correo@ejemplo.com
